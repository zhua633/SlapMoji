# Optional override; next.config.ts defaults to Railway production if unset.
NEXT_PUBLIC_API_URL=https://slapmoji-production.up.railway.app

# Supabase (create a project at https://supabase.com/dashboard — local: run `supabase start` and copy API URL + anon key)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
